# sample-plan-E101-E102.pdf — ground truth

Synthetic two-sheet electrical set, generated by `scripts/make_sample_plan.py`
(regenerate any time with `python scripts/make_sample_plan.py`). Drawn at a
**true 1/4" = 1'-0"** scale on ARCH D (36×24 in): 1 ft = 18 PDF points.

## Exact symbol counts

| Sheet | Symbol | Count |
|---|---|---|
| E-101 Power | Duplex receptacle (circle + 2 ticks) | **27** (15 bottom wall, 12 top wall) |
| E-101 Power | Panel `LP-1` (filled rect, right wall) | **1** |
| E-102 Lighting | 2'×4' troffer (rect + diagonal) | **50** (20 bottom offices, 24 top rooms, 6 corridor) |
| E-102 Lighting | Switch (`S`) | **9** (5 bottom doors, 4 top doors) |
| E-102 Lighting | Exit sign (small filled rect, corridor) | **4** |

The legend swatches (top-left box) are smaller than the plan symbols — a
correct auto-count should NOT include them. If a count comes back 28/51/10/5,
the detector picked up the legend.

## Calibration checks (known true values)

- The bottom dimension line's **arrow tips are exactly 100'-0" apart**
  (1800 pt). Calibrate by clicking the two arrow tips and entering `100`.
- The left dimension is exactly 60'-0".
- Building exterior: 100 ft × 60 ft. Bottom offices 20 ft wide × 24 ft deep;
  corridor 8 ft; top rooms 25 ft wide × 28 ft deep.
- Linear-takeoff check: the corridor centerline wall-to-wall is ~100 ft; one
  bottom office's bottom wall segment is 20 ft.

## Sheet-analysis (title block) expected answers

- Sheet numbers: `E-101`, `E-102`
- Sheet names: `FIRST FLOOR POWER PLAN`, `FIRST FLOOR LIGHTING PLAN`
- Scale text: `SCALE: 1/4" = 1'-0"` — printed exactly once per sheet, in the
  title block, so `parseDrawingScale` has an unambiguous read.
  At 1/4" = 1'-0", the correct derived scale is **1 PDF unit = 1/18 ft**
  (18 points per foot).

## Suggested test script

1. Upload the PDF in the Sheets panel (2 pages appear).
2. Run Sheet Analysis — proposals should match the table above; applying the
   scale proposal should equal the manual calibration.
3. Calibrate E-101 manually from the 100'-0" dimension; the two methods
   should agree.
4. Make a count layer "Receptacles", run AI auto-count, review the pending
   queue, compare accepted count to **27**.
5. Repeat on E-102 for troffers (**50**) and switches (**9**).
6. Link layers to priced items and confirm the estimate extends
   (e.g. 27 × item cost, to the cent).
